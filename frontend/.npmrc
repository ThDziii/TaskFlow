; Tăng thời gian chờ / số lần thử khi mạng chậm hoặc registry không ổn định
fetch-timeout=300000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

; Registry mặc định — nếu vẫn lỗi, thử mirror (xóa dòng dưới hoặc đổi sang mirror)
registry=https://registry.npmjs.org/
